---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-inspector-config
  namespace: {{ $.Release.Namespace }}
data:
  config.json: |
    {
      "mcpServers": {
        "mcp-alerts": {
          "type": "sse",
          "url": "http://mcp-alerts:3001/sse"
        }
      }
    }