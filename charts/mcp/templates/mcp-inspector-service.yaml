---
apiVersion: v1
kind: Service
metadata:
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    application: mcp-inspector
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http-mcp-inspector
