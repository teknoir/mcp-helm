---
apiVersion: v1
kind: Service
metadata:
  name: mcp-inspector
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    application: mcp-inspector
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http-mcp-inspector
